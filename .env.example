# OpenCode 远程服务器配置
# 这些配置会作为默认值，但可以被工具调用参数覆盖

# OpenCode 服务器地址（默认: http://localhost:8848）
OPENCODE_URL=http://100.72.207.100:8848

# 认证类型: bearer | basic | none（默认: basic）
# OpenCode serve 模式默认使用 basic auth，用户名固定为 "opencode"
OPENCODE_AUTH_TYPE=basic

# Basic Auth 认证（OpenCode serve 默认）
# 用户名固定为 "opencode"，密码是启动 serve 时设置的密码
OPENCODE_USERNAME=opencode
OPENCODE_PASSWORD=czy519116

# Bearer Token 认证（如果配置了 token）
# OPENCODE_AUTH_TYPE=bearer
# OPENCODE_TOKEN=your-token-here

# MCP Server 监听端口（SSE 模式，默认: 3000）
PORT=3000
