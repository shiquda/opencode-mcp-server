# OpenCode Remote Server Configuration
# These settings serve as defaults but can be overridden by tool call parameters

# OpenCode server address (default: http://127.0.0.1:4096)
OPENCODE_URL=http://127.0.0.1:4096

# Authentication type: bearer | basic | none (default: basic)
# OpenCode serve mode uses basic auth by default
OPENCODE_AUTH_TYPE=basic

# Basic Auth (default for opencode serve)
# Username is always "opencode", password is set when starting serve
OPENCODE_USERNAME=opencode
OPENCODE_PASSWORD=your-password

# Bearer Token authentication (if configured)
# OPENCODE_AUTH_TYPE=bearer
# OPENCODE_TOKEN=your-token-here

# MCP Server port (SSE mode, default: 3000)
PORT=3000
